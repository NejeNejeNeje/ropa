# ROPA — Environment Variables
# Copy this file to .env and fill in your values.
# Values marked (required) must be set before the app will run.
# Values marked (optional-wired) are pre-wired in code — the feature
# activates automatically the moment you add the credential.

# ── Required ───────────────────────────────────────────────────────────
# Neon PostgreSQL connection string (app won't start without this)
DATABASE_URL=""

# Random secret for session signing — generate with: npx auth secret
AUTH_SECRET=""

# Canonical URL of your deployment (no trailing slash)
AUTH_URL="https://yourdomain.com"

# ── Optional · Google Sign-In (optional-wired) ─────────────────────────
# 1. Go to console.cloud.google.com → APIs & Services → Credentials
# 2. Create OAuth 2.0 Client ID (Web Application)
# 3. Add Authorized redirect URI: https://yourdomain.com/api/auth/callback/google
# Once added, a "Continue with Google" button appears on the login page automatically.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ── Optional · Image Upload (optional-wired) ───────────────────────────
# 1. In your Vercel dashboard → Storage → Create Blob Store
# 2. Copy the token here.
# Once added, photo uploads in /listing/new are activated automatically.
BLOB_READ_WRITE_TOKEN=""

# ── Optional · Stripe Payments (optional-wired) ────────────────────────
# 1. Create account at stripe.com
# 2. Go to Dashboard → Developers → API keys
# Once added, paid offer checkout (/api/checkout) is activated automatically.
STRIPE_SECRET_KEY=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# ── Optional · Push Notifications (optional-wired) ────────────────────
# 1. Create a Firebase project at console.firebase.google.com
# 2. Enable Cloud Messaging, copy the Server Key and Web App Config
FIREBASE_SERVER_KEY=""
NEXT_PUBLIC_FIREBASE_CONFIG=""
